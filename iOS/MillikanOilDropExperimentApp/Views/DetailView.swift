import SwiftUI
import Foundation

struct DetailView: View {
    var body: some View {
        VStack {
            Text("Detailed Information View")
        }
        .navigationTitle("Details")
        .navigationBarTitleDisplayMode(.inline)
    }
}

